{
  "name": "harvest-safari-extension",
  "version": "1.1.0",
  "description": "Safari extension for Harvest time tracking",
  "scripts": {
    "build": "echo 'No build process needed for Safari extension'",
    "dev": "echo 'Load the extension folder directly in Safari'",
    "package": "zip -r harvest-safari-extension.zip . -x '*.git*' 'node_modules/*' '*.DS_Store'",
    "validate": "echo 'Validating extension structure...' && ls -la manifest.json popup.html",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "build:icons": "node scripts/build-icons.js",
    "prepackage": "npm run build:icons",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "safari",
    "extension",
    "harvest",
    "time-tracking",
    "productivity"
  ],
  "author": "Developer",
  "license": "MIT",
  "devDependencies": {
    "oxlint": "^1.16.0",
    "sharp": "^0.33.5",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/username/harvest-safari-extension"
  }
}
